server:
  port: 8081 # 商品服务占用 8081 端口

spring:
  application:
    name: product-service
  threads:
    virtual:
      enabled: true # 亮点：开启 Java 21 虚拟线程，支撑秒杀高并发！
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 【注意】把下面 rm-xxx... 换成你阿里云RDS的公网访问地址
    url: jdbc:mysql://rm-cn-swx4p26ot00019do.rwlb.rds.aliyuncs.com:3306/seckill_db?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: dms_user_9bc05ec # 换成你的数据库用户名
    password: AYAKO0217sir@ # 换成你的数据库密码

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true # 开启驼峰命名转换 (如 product_id 自动转为 productId)

logging:
  level:
    com.seckill: INFO # 设置我们项目的日志级别为 INFO，方便观察运行时日志输出