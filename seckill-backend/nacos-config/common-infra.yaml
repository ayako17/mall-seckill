# Data ID: common-infra.yaml
spring:
  data:
    redis:
      host: redis
      port: 6379
      database: 0
      timeout: 5000ms
  kafka:
    bootstrap-servers: kafka:9092

nacos:
  tip: Shared infra configuration for all services
